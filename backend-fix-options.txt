# Solución 1: Quitar sintaxis TypeScript de la línea problemática
# Cambia esta línea en index.js:
# const updates: any = {};
# Por esta:
# const updates = {};

# Solución 2: Configurar build para TypeScript
# Agregar a package.json:
# "scripts": {
#   "build": "tsc",
#   "start": "node dist/index.js"
# }

# Solución 3: Usar ts-node para ejecutar TypeScript directamente
# Cambiar package.json:
# "scripts": {
#   "start": "ts-node index.ts"
# }
# Instalar: npm install ts-node typescript @types/node

# Solución 4: Renombrar index.js a index.ts y configurar build
# Si usas TypeScript, el archivo debería ser .ts no .js